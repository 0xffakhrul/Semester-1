#include <iostream>
#include <math.h>
using namespace std;

int main ()
{
	double a,b,c,result,d;
	cout << " b ";
	cin >> b;
	cout << " a";
	cin >> a;
	cout << "c";
	cin >> c;
	cout << " d ";
	cin >> d;
	
	//result = (2*a - 3*b) / ( c - 2 ) - 5*( pow( d - 12, 3));
	//-b + (sqrt ( pow ( b,2 )) - (4 * a * c ) )/ ( 2 * a );
	//result = pow(a,3) - pow(b,5);
	//result = ( sqrt ( pow ( c,2 ) + 10 ));
	if ( a > 0 )
	   b += 10;
	else
	{
		if ( a > 5 )
		   b = a + 10;
	}
	
	b += 10;
	
	cout <<b;
	
	//cout << result;
	return 0;
}
