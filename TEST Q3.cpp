#include <iostream>
#include <math.h>

using namespace std;

int main ()
{
	float area_triangle, length, width, radius, shaded_region;
	
	/*cout << " Enter the length of the triangle : ";
	cin >> length;
	
	cout << " Enter the width of the triangle : ";
	cin >> width;
	
	area_triangle = (0.5)*length*width;
	
	radius = sqrt(2*area_triangle);
	
	shaded_region = (area_triangle*2)-radius;
	
	cout << " The shaded region is : " << shaded_region << endl;*/
	
	return 0;
}
